import React, {useState} from "react";
import './News.css';
import {FaChevronRight} from 'react-icons/fa';
import { ArticlesData } from "./NewsData";
import { useParams } from 'react-router-dom';
import { NewsPageCard } from "./NewsPage";
import NewsPage from "./NewsPage";
import NewsCards from "./NewsCards";
import GalleryCard from "./Gallery";


/*const gradient = "linear-gradient(360deg, rgba(57, 73, 87, 0.9) 0%, rgba(255, 255, 255, 0) 100%)"

const PageTest = ({ArticlesData}) => {

    // receber params
    const { index } = useParams();
   // console.log(param)
    // filter by index 

    */

 /*   return (
            <div className="container">
                {ArticlesData.filter((card) => card.index === index)
                .map((card, index) => (
                    <NewsPageCard />

                )
                )}
            </div>
                )}

        
        
export default PageTest  */

/*

{ArticlesData.filter((index) => param.index === index).map(
    (el) => console.log(el.title)
)} 

<div className="">
                    <span>
                        News / <span>{index}</span>
                    </span>
                    <div>
                        <img src="" alt="" />
                        <p>All news</p>
                    </div>
                </div> */